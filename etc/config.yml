server:
  capabilities:
    data_queries:
    - locations
    - items
    - area
    - position
    - radius

collections:
  json:
    cache_dir: "etc/data/collections_cache/"

data:
  interface:
    name: "dummy"
    path: null
