{% whitespace oneline %}
"{{ axis_name }}": {
  {% set points_count = len(points) %}
  {% if "values" in points.keys() %}
  "values": [
  {% for i, value in enumerate(points.values(), 1) %}
    "{{ value }}"{% if i < param_count %},{% end %}
  {% elif "start" in points.keys() %}
  {% for i, (key, value) in enumerate(points.items(), 1) %}
    "{{ key }}": "{{ value }}"{% if i < param_count %},{% end %}
  {% end %}
  {% end %}
  ]
  {% end %}
}